[build]
  command = "bun run build"
  publish = "dist"

# Nitro's "netlify" preset emits a Netlify Functions handler that serves SSR
# plus all static assets. No extra redirects are needed — the preset wires
# them up automatically during the build.